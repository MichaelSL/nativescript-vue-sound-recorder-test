// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.michaelsamorokov.nssoundrecordertest"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
